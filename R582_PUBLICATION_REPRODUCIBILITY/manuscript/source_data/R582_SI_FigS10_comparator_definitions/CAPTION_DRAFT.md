# Caption draft

**Figure S10 | Flat definitions of the single-fibre and pore-network comparator models.** **a,** Orthographic cross-sectional and axial views of the prescribed cylindrical unit cell (`r_f = 7.50 µm`, `r_out = 19.36 µm`, `L_z = 150 µm`, `epsilon = 0.85`). The normalized concentration is fixed at `c = 1` at the outer radius; the wall-state mask assigns `c = 0` to a bare wall node and zero normal flux to a covered wall node, with azimuthal and axial periodicity. The mask itself and the registered deposit-height array are not displayed. **b,** Exact `z = 7` slice of the registered `15 × 15 × 15` cubic pore network (225 nodes and 420 in-slice throats). Line width maps the registered initial throat radius `r0`; the two faces carry normalized Dirichlet values `phi = 1` and `phi = 0`. The lattice spacing is 60 µm, and the displayed equations define the hydraulic/ionic conductance scaling, normalized solid-allocation weights and throat-radius update. These `E-COMP` objects are prescribed model domains, not images or reconstructions of the experimental felt. No deposited-solid field, coverage image, solved pressure or potential field, flux field, updated-radius field or pore-blocking front is shown.
