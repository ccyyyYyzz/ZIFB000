# R582 Figure 2 v2 contract

Core conclusion: The continuum model resolves a two-dimensional ZIFB positive-electrode domain and separates free-I2 supersaturation, retained solid, calibrated remaining accessibility, the native reaction-area product, and voltage response.

Figure archetype: editorial schematic-led composite.

Final size: 180 mm x 90 mm.

Typography: TeX Gyre Termes is registered directly from the TeX Live OpenType files. Matplotlib math text uses the same family in roman, italic, and bold. No Arial, Helvetica, DejaVu Sans, or Times New Roman fallback is allowed.

Panel map:

- a: Minimal collector-positive-felt-separator cross-section, coordinates, and one electrolyte-flow arrow.
- b: One continuous unboxed state rail with five directly labelled nodes, `S -> eps_s -> R_theta -> A_bare/A0 -> V`. It defines `R_theta = 1 - theta_cal` and the native identity `A_bare/A0 = R_theta T_pore`; the dashed voltage-calibration path enters only `R_theta`.
- c: One thin capacity rail with the displayed markers `Q_s = 83.0` and `Q_f,cal = 99.6 mAh cm^-2` and exactly three direct state annotations.

Visual hierarchy: panel b is the hero definition; panels a and c provide domain and marker context. Colored underlines and ticks carry state identity without cards, pictograms, or decorative chemistry.

Integrity boundary: all marks are flat vector primitives. No particle, crystal, film, pore-blockage, deposit texture, inferred morphology, or NH4Br/Br- element appears. `1 - theta_cal` must never be relabelled as the full native `A_bare/A0` product.
