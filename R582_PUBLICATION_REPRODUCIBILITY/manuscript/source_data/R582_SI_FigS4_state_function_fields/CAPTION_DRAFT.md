**Figure S4 | Six-capacity inventory of modeled positive-electrode state and reaction fields.** Columns show the registered baseline simulation at areal capacities of 0, 80, 96, 100, 110 and 120 mAh cm^-2. **a,** Free-I2 stress, `S`, on a shared scale centred at the saturation threshold `S = 1`. **b,** Retained-solid fraction, `epsilon_s`, on a shared symlogarithmic percentage scale. **c,** Native remaining bare-area fraction, `A_bare/A0`, on a shared linear scale. **d,** Total reaction-current density, `j_total`, with positive values on a shared logarithmic scale. Non-positive current nodes are displayed by a neutral mask, without taking absolute values; their counts in column order are 248, 0, 33, 6, 0 and 0, and their signed values remain in the source table. Every map contains the same 5,995 exported nodes and uses nearest-neighbour rendering without interpolation, smoothing or clipping. Coordinates span the positive electrode from collector (`x = 3 mm`) to separator (`x = 5 mm`) and the 20 mm flow direction. These deterministic continuum fields are not microscopy or reconstructions of deposit morphology, coverage, a blockage front or electrical potential.

