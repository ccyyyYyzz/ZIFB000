**Figure S5 | Six-capacity inventory of modeled positive-electrode hydraulic fields.** Columns show the registered baseline simulation at areal capacities of 0, 80, 96, 100, 110 and 120 mAh cm^-2. **a,** Smooth relative permeability, `K/K0`. **b,** Native velocity magnitude, `|u|`, in m s^-1. **c,** Hydraulic pressure, `p`, in Pa relative to the registered outlet condition `p_out = 0`. Each row uses one fixed linear scale across all six capacities; every map contains the same 5,995 exported nodes and uses nearest-neighbour rendering without interpolation, smoothing or clipping. Coordinates span the positive electrode from collector (`x = 3 mm`) to separator (`x = 5 mm`) and the 20 mm flow direction. The pressure field is hydraulic, not electrical potential, and the smooth permeability channel does not resolve local pore closure or blockage.

